import 'package:flutter/material.dart';

class PersonalizarPedidos extends StatelessWidget {
  const PersonalizarPedidos({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Personalizar'));
  }
}
