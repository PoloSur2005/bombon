import 'package:flutter/material.dart';

class Personalizar extends StatelessWidget {
  const Personalizar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Personalizar'));
  }
}
